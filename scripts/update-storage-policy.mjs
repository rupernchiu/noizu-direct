import { createRequire } from 'module'
import { fileURLToPath } from 'url'
import path from 'path'

const require = createRequire(import.meta.url)
const __dirname = path.dirname(fileURLToPath(import.meta.url))
const rootDir = path.resolve(__dirname, '..')

const dotenv = require('dotenv')
dotenv.config({ path: path.join(rootDir, '.env') })
dotenv.config({ path: path.join(rootDir, '.env.local'), override: false })

const { Client } = require('pg')
const client = new Client({ connectionString: process.env.DATABASE_URL_DIRECT })
await client.connect()

const TITLE = 'Storage Policy'
const SEO_TITLE = 'Storage Policy — noizu.direct'
const SEO_DESCRIPTION =
  'Where noizu.direct stores data and creator files, how we encrypt and back it up, and retention for each data type.'

// The hero, "Where data lives" grid, and retention tiers render as hardcoded
// React in src/app/storage-policy/page.tsx. Only the detailed prose below
// flows through the CMS.
const CONTENT = `<blockquote>
<p><strong>In short.</strong> Your account data lives in Malaysia, creator files live in encrypted object storage, and a disaster-recovery copy sits in the European Economic Area. We encrypt sensitive data at rest, encrypt everything in transit, keep daily backups for thirty days, and delete what we no longer need. Anything you place in your account can be exported or deleted on request.</p>
</blockquote>

<p><em>This Storage Policy took effect on <strong>22 April 2026</strong> and supersedes all prior versions. It is a companion document to the <a href="/privacy">Privacy Policy</a>; the Privacy Policy controls whenever the two documents appear to disagree.</em></p>

<h2>Scope</h2>
<p>This Storage Policy describes where noizu.direct stores the information it processes, the technical and organisational safeguards we apply to that storage, how long each category of information is retained, and how data can be exported or deleted. It applies to all information processed through noizu.direct, whether submitted by buyers, creators, or general visitors, and complements the broader commitments set out in our Privacy Policy.</p>

<h2>Storage regions</h2>
<p>Our primary production infrastructure is hosted with providers operating in the <strong>Asia-Pacific region</strong>, with the principal production database and active object storage physically located in Malaysia. Disaster-recovery replicas of both the relational database and critical object-storage buckets are maintained in the <strong>European Economic Area</strong>. The EEA replicas are encrypted, segregated from production traffic, and used solely for recovery in the event of a primary-region outage.</p>
<p>When personal information is transferred outside the region in which it was collected &mdash; for example, for disaster-recovery replication into the EEA &mdash; we rely on contractual safeguards consistent with the Malaysia Personal Data Protection Act 2010 and, for transfers that concern data originally collected in the European Economic Area or the United Kingdom, on the European Commission&rsquo;s Standard Contractual Clauses together with any supplementary measures appropriate to the receiving jurisdiction.</p>

<h2>What we store, and where</h2>
<p>We operate four distinct storage systems, each with an access policy tuned to the sensitivity of what it contains.</p>
<h3>Relational database (Postgres)</h3>
<p>Account records, listing metadata, order and invoice history, commission state, message threads, and platform configuration are stored in a managed Postgres cluster. The cluster has point-in-time recovery enabled, and administrative access is restricted to a small number of named engineers who authenticate using hardware-backed two-factor authentication.</p>
<h3>Object storage (creator files)</h3>
<p>Product images, digital goods, commission deliverables, and other creator-uploaded assets are stored in encrypted object-storage buckets. Each object is addressable only through a short-lived signed URL generated by the platform; there is no public listing of bucket contents. Digital goods delivered to buyers are protected by expiring download tokens scoped to the purchasing account.</p>
<h3>Identity-verification (KYC) vault</h3>
<p>Government-issued identification and selfie verification images required for creator payouts are stored in a <strong>separate, access-restricted bucket</strong> with its own encryption context and its own retention rules. Access to the KYC vault is limited to the trust-and-safety function and is logged in an append-only audit trail.</p>
<h3>Operational logs and analytics</h3>
<p>Application logs, request traces, and aggregated analytics are kept to monitor performance and investigate incidents. Log data is scrubbed of sensitive fields before storage and is retained for no longer than ninety days except where a specific investigation or legal hold requires otherwise.</p>

<h2>Encryption</h2>
<p>All traffic between your browser and our servers is protected by TLS. At rest, the Postgres cluster and all object-storage buckets are encrypted at the storage-platform level. In addition, particularly sensitive fields &mdash; password hashes, KYC document references, payment-token identifiers &mdash; are stored under an application-level encryption envelope so that recovery of a raw storage volume alone would not yield readable values.</p>

<h2>Backups and disaster recovery</h2>
<p>The production database is backed up continuously, with recovery points at five-minute granularity for the past seven days and daily snapshots retained for thirty days. Object storage is replicated asynchronously to the disaster-recovery region. Backup-restoration drills are performed on a quarterly cadence; the results of those drills are reviewed by the engineering team and fed back into our recovery playbooks.</p>
<p>Our target recovery objectives for a primary-region outage are a recovery-point objective (<strong>RPO</strong>) of fifteen minutes for relational data and a recovery-time objective (<strong>RTO</strong>) of four hours to restore core marketplace functionality. These are targets, not warranties &mdash; see the <a href="/terms">Terms of Service</a> for the applicable limitations of liability.</p>

<h2>Access controls</h2>
<ul>
<li><strong>Principle of least privilege.</strong> Engineers receive the narrowest role that allows them to do their job; shared service accounts with broad privileges are not used.</li>
<li><strong>Two-factor authentication.</strong> Mandatory for every staff member with production access, enforced at the identity provider.</li>
<li><strong>Auditable administrative actions.</strong> Every privileged action &mdash; schema change, production query, KYC-vault access &mdash; is logged to an immutable audit trail reviewable by the trust-and-safety team.</li>
<li><strong>Break-glass procedure.</strong> Emergency production access outside regular roles requires a paired second approver and is logged and reviewed after the fact.</li>
</ul>

<h2>Retention</h2>
<p>We retain information only for as long as it is needed for the purpose for which it was collected, or for as long as we are required to keep it under applicable law. The headline durations are:</p>
<ul>
<li><strong>Account records</strong> &mdash; for as long as the account remains open; archived within thirty days of closure, then purged.</li>
<li><strong>Order and invoice records</strong> &mdash; <strong>seven years</strong>, as required by Malaysian accounting and tax law.</li>
<li><strong>KYC documentation</strong> &mdash; <strong>eighteen months</strong> after the closure of the associated creator account, to defend against later chargebacks and fraud investigations.</li>
<li><strong>Support correspondence and dispute evidence</strong> &mdash; <strong>thirty-six months</strong> from the last interaction.</li>
<li><strong>Daily database backups</strong> &mdash; <strong>thirty days</strong>, on a rolling overwrite basis.</li>
<li><strong>Operational logs</strong> &mdash; <strong>ninety days</strong>, except under a specific legal hold.</li>
<li><strong>Soft-deleted content</strong> &mdash; removed from active systems immediately and purged from backups within <strong>thirty days</strong>.</li>
</ul>
<p>When a retention period ends, records are either securely deleted or &mdash; where they must be retained in a limited form for legal reasons &mdash; anonymised so that they no longer identify a living individual.</p>

<h2>Deletion and export</h2>
<p>You can request a <strong>data export</strong> or a <strong>data deletion</strong> from your account settings, or by writing to <a href="mailto:privacy@noizu.direct">privacy@noizu.direct</a>. Exports are delivered as a machine-readable ZIP within thirty days. Deletion requests are completed within thirty days, subject to the retention periods above for records we are legally required to keep.</p>
<p>When you delete an individual listing or digital file from your dashboard, it is immediately removed from the active marketplace and from public object-storage URLs. The underlying object persists in cold storage for up to thirty days to permit restoration in case of accidental deletion, and is then permanently purged.</p>

<h2>Sub-processors</h2>
<p>We rely on a small, audited set of sub-processors for hosting, object storage, payment processing, transactional email, and customer-support tooling. Each sub-processor is engaged under a written data-processing agreement that requires protection equivalent to this Policy. A current list of sub-processors, the region in which each operates, and the category of data processed is available on request to <a href="mailto:privacy@noizu.direct">privacy@noizu.direct</a>.</p>

<h2>Incidents and notification</h2>
<p>If we become aware of a confirmed unauthorised disclosure of or access to personal information stored in our systems, we will notify affected users without undue delay and, where required, within the timeframe set out by applicable law. Notifications describe, to the extent known at the time: the nature of the incident, the categories of information affected, the measures we have taken, and the steps users can take to protect themselves.</p>

<h2>Changes to this policy</h2>
<p>We may amend this Storage Policy from time to time to reflect changes in the infrastructure, in our service providers, or in applicable law. Material amendments &mdash; including any change to the primary storage region, a change in sub-processor, or a material change to retention duration &mdash; will be notified to registered users by email at least <strong>fourteen days</strong> before they take effect. Minor clarifications may be reflected by updating the effective date at the top of this Policy.</p>`

const beforeRes = await client.query(
  `SELECT id, title, LENGTH(COALESCE(content,'')) AS len FROM "Page" WHERE slug=$1`,
  ['storage-policy']
)
console.log('BEFORE:', beforeRes.rows[0] ?? '(no row)')

const res = await client.query(
  `INSERT INTO "Page" (id, slug, title, content, "seoTitle", "seoDescription", status, "showInFooter", "footerColumn", "footerOrder", "updatedAt")
   VALUES (gen_random_uuid()::text, $1, $2, $3, $4, $5, 'PUBLISHED', TRUE, 'Support', 50, NOW())
   ON CONFLICT (slug) DO UPDATE
     SET title = EXCLUDED.title,
         content = EXCLUDED.content,
         "seoTitle" = EXCLUDED."seoTitle",
         "seoDescription" = EXCLUDED."seoDescription",
         status = 'PUBLISHED',
         "showInFooter" = TRUE,
         "footerColumn" = COALESCE("Page"."footerColumn", 'Support'),
         "updatedAt" = NOW()
   RETURNING id, slug, LENGTH(content) AS len, "seoTitle"`,
  ['storage-policy', TITLE, CONTENT, SEO_TITLE, SEO_DESCRIPTION]
)
console.log('AFTER:', res.rows[0])

await client.end()
