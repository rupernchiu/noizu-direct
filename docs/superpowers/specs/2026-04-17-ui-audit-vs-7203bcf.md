# UI Audit — HEAD vs Savepoint 7203bcf

**Savepoint:** `7203bcf` — "savepoint: staff permissions, creator health, notifications, forgot password, logo, wishlist pill"  
**Audit date:** 2026-04-17  
**Files changed (UI-touching):** 15 components/pages out of 80 total changed files

---

## Summary

Changes since the savepoint are predominantly intentional improvements: trending/discovery features, a full review system, social proof signals, and creator store status gates. Two regressions were identified (SEO-related), and one breaking change (payment processor migration to Airwallex).

---

## Component-by-Component Breakdown

### Home Page — `src/app/page.tsx`
- **Added:** "Trending Now" section inserted before "How It Works"
- **Assessment:** ✅ Intentional improvement — no regressions

---

### Navbar — `src/components/layout/Navbar.tsx`
- **Changed:** Role-specific links ("Creator Dashboard", "Admin Panel") replaced with unified "My Dashboard" that routes by user role
- **Added:** Role badge (Admin / Creator / Member) in dropdown and mobile menu
- **Added:** Notifications menu item (hidden for admins)
- **Assessment:** ✅ Intentional improvement. Minor concern: admins lose the explicit "Admin Panel" label — may reduce discoverability. Monitor if admin users report confusion.

---

### Blog Post — `src/app/blog/[slug]/page.tsx`
- **Changed:** JSON-LD author field hardcoded to `'NOIZU-DIRECT'` instead of `post.author?.name`
- **Assessment:** ⚠️ **Regression Risk** — author attribution removed from SEO structured data. Should restore dynamic author name.

---

### Blog List — `src/app/blog/page.tsx`
- **Changed:** Backend caching added only
- **Assessment:** ✅ No UI change

---

### Marketplace — `src/app/marketplace/MarketplaceClient.tsx`
- **Added:** "Trending" sort option in sort dropdown
- **Assessment:** ✅ Intentional improvement — aligns with new trending infrastructure

---

### Creator Store Page — `src/app/creator/[username]/page.tsx`
- **Added:** FLAGGED/IDLE store status shows full-page block: warning icon, "Store Unavailable" message, link to browse other creators
- **Added:** Amber hiatus banner when status is HIATUS: "This store is currently on hiatus. The creator may return soon."
- **Added:** "Fans also love" section — 4 related creators based on recommendation data (only renders if 2+ found)
- **Assessment:** ✅ Intentional improvements — no regressions

---

### Creator Page Tabs — `src/app/creator/[username]/CreatorPageTabs.tsx`
- **Added:** Sort toggle (Default | Popular) in shop section header
- **Added:** Popular view: 4-column grid ordered by `trendingScore`
- **Added:** Star ratings (★★★☆☆ format + review count) below products in both sort modes
- **Assessment:** ✅ Intentional improvement. Note: Popular sort removes the pinned/unpinned product distinction — intentional UX trade-off, acceptable.

---

### Product Detail — `src/app/product/[id]/page.tsx`
- **Added:** Star rating + review count below product title (shown only if reviews exist)
- **Added:** "Buyers also purchased" horizontally-scrollable carousel (6 products, renders only if 2+ recommendations)
- **Added:** Full review section: rating summary card, 5-star distribution bar chart, individual review cards with reviewer avatar, verified-purchase badge, date, title, and body
- **Added:** `ProductViewTracker` for analytics
- **Assessment:** ✅ Major intentional improvement — significant trust and social proof additions

---

### Search — `src/app/search/page.tsx` + `src/app/search/SearchResults.tsx`
- **Overhauled:** Server-rendered → real-time client-side component with 300ms debounce
- **Added:** Live result count, tab-based filtering (Products | Creators | Posts), per-tab pagination
- **Changed:** Grid layouts — Products: 6-col, Creators: 5-col, Posts: 3-col
- **Assessment:** ⚠️ **Regression Risk (SEO)** — search results are now client-side only. Search engine crawlers cannot index result pages. If search SEO matters, consider adding SSR fallback or static metadata.

---

### Checkout — `src/app/checkout/CheckoutPageClient.tsx`
- **Breaking:** Entire payment flow replaced — session-based redirect flow removed, replaced with inline Airwallex drop-in widget mounted at `#airwallex-dropin`
- **Changed:** "Proceed to Payment" button hidden once Airwallex state is set; widget, inline error messages, and "Continue Shopping" button shown instead
- **Assessment:** 🔴 **Breaking Change** — intentional payment processor migration to Airwallex. Verify `AIRWALLEX_*` env vars are correctly set in production and the drop-in widget loads without errors.

---

### Dashboard — `src/app/dashboard/page.tsx`
- **Added:** "Complete store profile" onboarding step (requires `displayName` + `username`; links to `/dashboard/profile`)
- **Changed:** Onboarding dismissed state now persisted via API instead of `localStorage` only
- **Assessment:** ✅ Intentional improvement

---

### Onboarding Components — `OnboardingChecklist.tsx` + `OnboardingChecklistWrapper.tsx`
- **Changed:** On completion, shows "🎉 Your store is ready!" success card instead of checklist silently disappearing
- **Changed:** Dismiss/complete state saved to API endpoints (`/api/dashboard/onboarding/complete`, `/api/dashboard/onboarding/dismiss`) — survives page reloads
- **Assessment:** ✅ Intentional improvement

---

### Trending Section — `src/components/sections/TrendingSection.tsx`
- **New component:** 8 trending products in 4-column responsive grid (2 mobile, 3 tablet, 4 desktop) with "View all →" link; falls back to most recent products if no trending data
- **Assessment:** ✅ New feature, no regressions

---

## Issues Requiring Action

| Priority | Component | Issue |
|----------|-----------|-------|
| Medium | `blog/[slug]/page.tsx` | Author removed from JSON-LD — restore `post.author?.name \|\| 'NOIZU-DIRECT'` |
| Medium | `search/` | Client-side search loses SSR crawlability — evaluate if search SEO is needed |
| Verify | `checkout/` | Airwallex drop-in live in production — confirm widget loads and payments succeed end-to-end |

---

## No Action Needed

Navbar consolidation, creator status gates, hiatus banner, related creator discovery, popular sort, star ratings, full review system, trending section, onboarding improvements — all intentional and positive changes.
